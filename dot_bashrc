# Common aliases
alias ll='ls -la'

# Load direnv if available
if command -v direnv &> /dev/null; then
  eval "$(direnv hook bash)"
fi

# Load 1Password CLI if available
if command -v op &> /dev/null; then
  export OPCLI=1
fi
